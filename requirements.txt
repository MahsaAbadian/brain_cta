SimpleITK
# NumPy 2.0 removes np.float_; TopBrain eval stack (evalutils, etc.) expects 1.x
numpy>=1.25.2,<2.0
scipy
pytest
nibabel
# TopBrain runtime deps (match TopBrain_Eval_Metrics-master/pyproject.toml)
evalutils==0.4.2
pandas==2.0.3
scikit-image==0.21.0
# Training (install before running src/train.py)
torch
